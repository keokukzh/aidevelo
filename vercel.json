{
  "version": 2,
  "buildCommand": "pnpm -r build && mkdir -p ./dist && cp -r ui/dist/* ./dist/",
  "installCommand": "pnpm install --prod=false",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
